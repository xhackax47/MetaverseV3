config = {
    -- How many seconds to wait for vehicle to initilize before timing out
    spawnTimeout = 30,

    -- Distance to player before considering spawning a vehicle?
    spawnDistance = 50.0,

    -- For debug prints
    debug = false,
}